# One original layer3 finite FIFO observation

Reuse independently accepted layer3-fifo-trace-compile-002:
artifact10000512 bytes, SHA256ccf1a39ad563b6e92ded907e23357e11ea74cf4cba9b3fbbef7c6f01738db975.
Its563 actual programs cover33750PEs including30576 matrix PEs.
Maximum ordinary section end plus the original4096-byte stack is47920 bytes,
leaving208 bytes below48128. Actual ELF inspection proves all original
allocations, separate4-byte headers/124-byte payloads, six FIFO source arrays,
all50 real640-byte observer locations, and required live-array disjointness.
This frozen runtime requires its own exact one-attempt admission.

The original 394-copy/three-launch sequence is retained. Only its last transfer
changes to the idle rectangle (748,0,2,25),160u32 per PE,8000 words/32000 bytes.
At column748 retain the original25 independent32-word records and verify their
128-word zero padding. At column749 decode six identified raw finite FIFO
prefixes; verify the other19 complete160-word regions remain zero. Preserve raw
words and malformed observations. There is one read20s after compute returns,
a35s absolute observer window and unconditional stop. No busy PE is read after
compute, and no outside-ROI SDK callback is used as a release trigger.

All original parameters and pre-compute checks remain unchanged. Total host
transfer bytes1508898048, maximum single transfer14598144. No extra epoch,
parameter readback, host neural feedback, retries or mathematical audit follows.
The original write-only QK record alias makes the retained RMS audit invalid.

FIFO observations are at most404 events/1616 event bytes, separately coherent
per sink. They are not globally simultaneous. They cannot prove an absent event's
unique cause, actual source counters, absence of later source bound violation,
SDK output stall, layer completion or neural correctness. Maximum-count traces
retain these limitations. Existing packet/header/payload and sideband ordering,
mathematics, caches, ownership barriers and model preparation remain original.

Preserve the existing600s runtime,30s progress,2CPU/4GiB worker,4GiB host AS,
1GiB RSS,128MiB file,8MiB log,64MiB candidate and32GiB disk reserve limits.
The watchdog is byte-identical to the accepted baseline. All actual artifacts,
NPZ arrays, weights and logs remain on ALCF. Only bounded receipt metadata returns
to Mac. New full diagnostic receipts are bounded at256KiB individually; summaries
must not truncate original evidence. Release and reap every owned process/job,
then freshly verify no device assignment remains. Preserve every failure.
