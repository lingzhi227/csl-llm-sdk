const memcpy=@import_module("<memcpy/get_params>",.{.width=1,.height=1});
layout{
 @set_rectangle(1,1);@set_tile_code(0,0,"pe.csl",.{.memcpy_params=memcpy.get_params(0)});
 @export_name("staging",[*]f32,false);@export_name("gain_output_storage",[*]u16,false);
 @export_name("stats",[*]f32,false);
 @export_name("control",[*]u32,false);@export_name("state",[*]u32,false);
 @export_name("probe_input",[*]u32,false);@export_name("probe_output",[*]u16,false);
 @export_name("timing",[*]u16,false);
 @export_name("compute",fn()void);@export_name("convert_probe",fn()void);
}
