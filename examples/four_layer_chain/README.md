# Four original layers and fresh full-state restoration

[Device](device) · [Baseline runtime](baseline) · [Fresh restore runtime](fresh_restore) ·
[Resumed audit](resumed_audit) · [Finalizer](finalizer) ·
[Physical report](../../docs/FOUR-LAYER-FRESH-RESTORE.md).

Each source-map entry is byte-identical to its frozen historical candidate.
The report records the conditional numerical scope,39-bank physical restore,
nominal differences, original failures and 60.486867-second parent-monitor gap.
These archives are not a portable one-command launcher. Deployment-specific
manifests/inputs/admissions and the large generated host-plan are omitted;
the resumed audit's machine-specific gate is also omitted. Reproduction needs
qualified original data, regenerated bindings and a fresh resource admission.
No full 64/fullvocabulary CSL generation or matched performance is claimed.
