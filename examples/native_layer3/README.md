# Complete native layer3 graph

These two compile-source snapshots retain all33,750 original application PEs,
30,576 matrix PEs,24 attention heads,24 archive sinks,136 MLP owners and16 native
KV source roots. No tile is demoted. Both complete graphs passed independent
actual ELF placement, original-bank storage and SRAM inspection. Maximum ordinary
storage plus4 KiB stack allowance is47,552 bytes against48,128; dynamic stack
peak and original-layer numerical correctness are separate acceptance gates.

Fullgraph001 restores the complete layout after the earlier selected-program
fit. Fullgraph002 adds origin-only first-error diagnostics: a44-byte immutable
first-failure record, distinct reason codes and error-only observer schema5.
All32 other CSL files, including layout, mathematical kernels, packet transport,
frame logic and head archive code, match001. This identifies a protocol failure;
it is not a claimed repair or a complete neural-layer acceptance.

[Compile evidence](../../evidence/native-layer3/compile-summary.json) and
[source provenance](../../evidence/native-layer3/source-map.json) bind all67
published CSL files to their frozen sources. Only the two compiler entry points
have a portability edit: set CSL_INSPECT_PYTHON to the installed interpreter that
provides cerebras.elf. No device code changes in publication.

SDK2.10.1 physical compilation used one worker CPU/4 GiB and a600-second limit.
The source preserves the original role map, placement inspector and guarded
entry points. Reproduction requires site-local SDK access, independent resource
review, a fresh source manifest and exact single-attempt admission. No historical
artifact, weights, credentials, scheduler logs or ready-to-submit admission is
included. Runtime and full-model inference remain separately qualified work.

[Full report](../../docs/NATIVE-LAYER3-GRAPH.md) records both failed physical
attempts and the exact malformed READY. Compilation does not imply a completed
original neural layer; complete original neural epochs remain zero.
